type: route-rule
name: loyalty-level-debug
spec:
  destination: loyalty-level.default.svc.cluster.local
  precedence: 2
  match:
    httpHeaders:
      cookie:
        regex: "^(.*?;)?(user=jalcorn@us.ibm.com)(;.*)?$"
  route:
  - tags:
      version: v2
